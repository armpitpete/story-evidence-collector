---
completion_authority: true
standard: Recursive Project Improvement Standard v1.0
status: AUTHORISED
authority_ref: main
---

# Story Evidence Collector — Current Status

## Current authority

- Repository: `armpitpete/story-evidence-collector`
- Governing branch: `main`
- Exact current main: `f286ce93f6eafaadfa76123f1e23cdd726e56672`
- PR #231 merged at exact reviewed head `744acd6c060e13138da0510ebc85a1c9820d1c24`.
- PR #231 merge commit: `f286ce93f6eafaadfa76123f1e23cdd726e56672`.
- The Complete MP report and analysis architecture contract v1 remains authoritative.
- All previously accepted Complete MP Report evidence, fixture and regression baselines remain authoritative.

### Governing project purpose

> Build a complete, traceable public evidence record for each MP, then use that evidence to reveal significant patterns, relationships, changes and inconsistencies that are difficult to see through ordinary human reading—without overstating what the evidence proves.

Every later lane must materially improve evidence completeness, traceability, reproducibility or interpretation safety. A workflow guard must protect an accepted baseline without permanently blocking a later separately authorised fixture extension.

## Closed authority lane

The `STATUS.md`-only **oral-question inventory closure and neutral fixture-integration authority** lane is complete.

PR #231 recorded:

- closure of the merged current-Parliament tabled oral questions fixed source-inventory lane;
- exact fixed capture `2026-07-23T17:27:27Z`;
- 12 official member-query records;
- seven current-Parliament inclusions: `319088`, `319553`, `332039`, `373735`, `374007`, `404663`, `411372`;
- five explicit earlier exclusions: `122465`, `127241`, `271822`, `271823`, `287272`;
- raw response SHA-256 `9d3b31f77d8e53c018039713fad830bc1e99b73bc5ca7c6a7bd0d67603cd35f9`;
- canonical packet payload SHA-256 `13c94b8044c54415e6f04c307f79f5a5a0935afe8240121caef3c805eb194fb5`;
- one later neutral three-file fixture-integration lane.

No fixture implementation, classification, interpretation, relationship, analysis, section completion, public output, publication or deployment was performed in PR #231.

## Recorded stop condition

Pre-implementation verification exposed three inherited workflow-scope guards that reject the correctly authorised future three-file fixture integration before its substantive regressions can be accepted.

The blocking workflows are exactly:

- `.github/workflows/jeremy-corbyn-current-parliament-early-day-motions-fixture-integration-test.yml`
- `.github/workflows/jeremy-corbyn-current-parliament-commitments-public-positions-test.yml`
- `.github/workflows/jeremy-corbyn-current-parliament-spoken-contributions-test.yml`

The blocker is mechanical, not evidential:

1. the Early Day Motions fixture workflow requires PR number `220` and the historical five-file PR #220 set;
2. the commitments/public-positions workflow accepts only its original implementation set or the historical PR #220 five-file set;
3. the spoken-contributions workflow accepts only its original implementation set and named historic repair/integration exceptions, including PR #220.

All three workflows trigger when `fixtures/complete-mp-reports/jeremy-corbyn-fixture-v1.json` changes. Therefore the authorised oral-question three-file implementation would be rejected solely because it is a later separately authorised fixture extension.

The fixture-integration implementation has not begun. No fixture, inventory, source note, source-interface proof, schema, generator, regression, workflow, server or database implementation file has been changed in this authority record.

## Current bounded prerequisite lane

One bounded **oral-question fixture workflow-scope guard repair** lane is selected and authorised only after this `STATUS.md`-only authority record merges and the resulting exact `main` is verified.

### Exact future repair scope

The later prerequisite repair PR may change exactly:

- `.github/workflows/jeremy-corbyn-current-parliament-early-day-motions-fixture-integration-test.yml`
- `.github/workflows/jeremy-corbyn-current-parliament-commitments-public-positions-test.yml`
- `.github/workflows/jeremy-corbyn-current-parliament-spoken-contributions-test.yml`

No other file is authorised.

### Repair contract

The later repair must:

1. preserve every existing trigger, read-only permission, checkout, Python setup, compilation step, substantive regression, diagnostic and `git diff --check` step;
2. preserve every existing original-lane allowance and every named historic exact PR, head and file-set allowance;
3. add only one new future-scope allowance for the exact oral-question fixture-integration three-file set:
   - `.github/workflows/jeremy-corbyn-current-parliament-tabled-oral-questions-fixture-integration-test.yml`
   - `fixtures/complete-mp-reports/jeremy-corbyn-fixture-v1.json`
   - `scripts/test_jeremy_corbyn_current_parliament_tabled_oral_questions_fixture_integration_v1.py`;
4. require the changed-file set to equal that exact sorted three-file set before the new allowance can pass;
5. create no branch wildcard, title-based exception, broad fixture allowance, skipped test, silent-success path or general PR-number bypass;
6. leave all substantive historical regressions unchanged;
7. remain read-only and make no fixture, evidence, source, schema, generator, server, database or publication change.

### Required validation

Before the prerequisite repair can close, require:

- valid workflow YAML for all three changed files;
- exact three-file base-to-head scope;
- proof that every existing allowance remains textually and behaviourally present;
- proof that the new allowance recognises only the exact oral-question fixture-integration three-file set;
- negative proof that a missing, additional or substituted file fails each guard;
- preservation of all existing substantive workflow steps;
- `git diff --check`;
- Project Control;
- Repository Release validation;
- a draft PR at an exact head for owner review.

### Failure and stop conditions

Stop the prerequisite repair if:

- the exact base, head, three-workflow scope or governing authority changes;
- any substantive regression, trigger, permission or checkout behaviour must change;
- an existing historic allowance would be removed or widened;
- the oral-question allowance cannot be expressed as the exact three-file set;
- a workflow would need write permission;
- any fixture, evidence, regression, source, schema, generator, server, database or public-output file must change;
- the repair would itself begin the oral-question fixture integration.

A stop must be recorded visibly. Do not relax or disable a guard merely to obtain a green result.

## Parked authorised implementation

After the prerequisite workflow repair is merged and the resulting exact `main` is verified, the previously authorised oral-question fixture-integration lane may resume unchanged.

Its exact implementation scope remains:

- `fixtures/complete-mp-reports/jeremy-corbyn-fixture-v1.json`
- `scripts/test_jeremy_corbyn_current_parliament_tabled_oral_questions_fixture_integration_v1.py`
- `.github/workflows/jeremy-corbyn-current-parliament-tabled-oral-questions-fixture-integration-test.yml`

Its evidence, neutrality, checksum, preservation, validation, stop-condition and publication boundaries remain exactly as recorded by merged PR #231. This authority record does not expand or otherwise alter that contract.

## Explicit exclusions

This authority record and the later prerequisite repair do not authorise:

- beginning the three-file oral-question fixture implementation before the prerequisite repair merges and exact `main` is verified;
- fixture, source inventory, source note or source-interface changes;
- changes to Python regressions;
- schema, generator, server or database changes;
- classification or commitment/public-position promotion;
- relationships, conduct comparison, delivery or contradiction assessment;
- section completion or pristine acceptance;
- report production, public output, publication or deployment;
- generalisation to another MP.

## Done

- PR #231 merged at exact reviewed head `744acd6c060e13138da0510ebc85a1c9820d1c24`.
- Exact resulting main is `f286ce93f6eafaadfa76123f1e23cdd726e56672`.
- The oral-question source inventory is complete and the neutral fixture-integration contract is authoritative.
- The three inherited workflow blockers are identified exactly.
- One bounded three-workflow prerequisite repair is selected with exact validation and stop conditions.
- The fixture implementation remains unstarted and parked.

## To do

- merge this `STATUS.md`-only blocker-and-authority PR after Project Control passes;
- treat Repository Release validation as path-inapplicable if its current workflow does not trigger for `STATUS.md`;
- verify the resulting exact `main`;
- from that exact `main`, open one draft three-workflow prerequisite repair PR;
- preserve every existing guard and substantive step;
- add only the exact oral-question three-file implementation allowance;
- run every required validation;
- stop at an exact draft PR head for owner review.

## Next bounded gate

After this `STATUS.md`-only authority PR merges, verify the resulting exact `main`. From that exact `main`, open one draft three-workflow guard-repair PR using only the authorised scope. Add only the exact future oral-question three-file fixture-integration allowance, preserve every existing allowance and substantive step, run every required check, and stop at an exact PR head for owner review.

## Stop point

Do not begin the oral-question fixture-integration implementation before this authority record and the later three-workflow prerequisite repair are merged and their resulting exact `main` commits are verified. Do not modify the fixture, fixed inventory, source note, source-interface proof, Python regressions, schema, generator, server or database in this authority PR. Do not classify content, create findings, complete a section, produce public output, publish or deploy.
